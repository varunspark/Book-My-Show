apiVersion: v1
kind: Namespace
metadata:
  name: book-my-show
  labels:
    app: book-my-show
---
# Optional: resource quota (uncomment if you want to enforce quotas)
# apiVersion: v1
# kind: ResourceQuota
# metadata:
#   name: rq-book-my-show
#   namespace: book-my-show
# spec:
#   hard:
#     requests.cpu: "2"
#     requests.memory: 4Gi
#     limits.cpu: "4"
#     limits.memory: 8Gi
---
# Optional: limit range (uncomment if you want defaults/limits)
# apiVersion: v1
# kind: LimitRange
# metadata:
#   name: lr-book-my-show
#   namespace: book-my-show
# spec:
#   limits:
#   - default:
#       cpu: 500m
#       memory: 512Mi
#     defaultRequest:
#       cpu: 250m
#       memory: 256Mi
#     type: Container
